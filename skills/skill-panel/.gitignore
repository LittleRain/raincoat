# 通用代码目录里不该出现任何本地产物。
#
# 产物（扫描快照、仪表盘、plan 包）默认落在 $HOME/.skill-panel/，或由 --out /
# $SKILL_PANEL_OUT 指定；它们含本机路径与命中的凭据原文，绝不能进任何仓库。
# 下面这几条是兜底：万一有人把 --out 指到 skill 目录里，也拦得住。
# 规则放 skill 根下（而不是仓库根），这样导出成独立仓库时会跟着一起走。
data/
skill-panel.html
plan-*.md
plan-*.sh
__pycache__/
*.pyc
