version: 1
interface:
  display_name: Create Report Skill
  short_description: Normalize report requirements into validated reusable HTML report skills.
  default_prompt: |
    Use $create-report. Normalize the supplied report requirement against the
    packaged contract, stop on missing execution-critical facts, and generate or
    update the downstream report skill only after the output inventory and level
    evidence are valid.
