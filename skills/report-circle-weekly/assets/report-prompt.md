# 周报生成 Prompt

基于标准化 spec 和声明过的数据合同，生成最终 HTML 周报。

## 规则

- 严格按照 `assets/report-outline.md` 中的栏目顺序输出
- 只使用标准化 spec 中声明过的指标、维度和数据合同
- 解释 GMV 波动时，只能使用已声明证据，例如 GMV、订单、买家、曝光、
  CTR、转化率、GPM、贡献率
- 禁止输出没有数据支持的外部归因或根因判断
- 结尾必须附上数据说明和必要 caveat

## 输出要求

- 最终格式：HTML
- 必须包含标题区、周期区、栏目正文、数据说明区
